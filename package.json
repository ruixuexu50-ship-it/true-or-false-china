{
  "name": "public-site-v1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "node --test test/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "verify": "npm test && npm run typecheck && npm run build"
  },
  "dependencies": {
    "astro": "7.2.1",
    "posthog-js": "1.416.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
